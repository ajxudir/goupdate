{
    "name": "example/no-lock",
    "description": "Edge case: composer.json without composer.lock (LockMissing status)",
    "require": {
        "php": ">=8.1",
        "guzzlehttp/guzzle": "^7.0",
        "monolog/monolog": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0"
    }
}
